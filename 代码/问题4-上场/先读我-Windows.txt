问题4 上场 N19 框架 —— 只传这一份到 Windows

决策改成 N19（朝向位 + 19 点网）。v14 还在，回退用 run_official_v14.py。
先演练，不要点「问题4正式测试」。点了正式会扣次数。

先装：Python 3.10+ ，然后 pip install numpy

1. 解压后进入本文件夹。
2. 官方模拟器先登录，只点「问题4演练测试」，等5秒倒计时结束。
3. 不要点「问题4正式测试」。
4. 打开「命令提示符」或 PowerShell，把队号换成你们的：

   cd /d 本文件夹路径
   set CUMCM_ROBOT_ID=你们的参赛队号
   python run_official_smoke.py
   python run_official_n19.py

PowerShell 用：
   $env:CUMCM_ROBOT_ID="你们的参赛队号"

冒烟虚拟时间应约为 199 秒。
演练能正常退出、清除只数对得上，再点正式。点开始或中止都扣一次。

本地冒烟（不占官方次数，不连模拟器）：

   python run_problem4_v14.py --n 2 --workers 1 --pdir 0.5

回退：python run_official_v14.py （旧 v14 combo_opk）
再回退：problem4_belief_v13.py
