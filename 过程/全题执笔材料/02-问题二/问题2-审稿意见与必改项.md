# 问题 2 审稿意见与论文必改项（执笔必看）

> **说明**：微信里的《问题二论文.docx》经评审专家审查，**骨架成立，数据对齐，不是造数**。但正文有几处硬伤在整篇合并时**必须修正**，否则会被扣学术规范分。

---

## 1. 核心定稿数值（全文必须统一）

* **基准场景**：中心站 $S_1 = (0, 0)$，第一条示向度 $	heta_1 = 0^\circ$（正东方向）。
* **最优第二检测点**：
  * 对称坐标：$(844, 544)\text{ m}$ 或 $(844, -544)\text{ m}$；
  * 极坐标形式：距离 $S_1$ 为 **1004 米**，偏开角度为 **±32.8°**；
  * 最小最坏定位直径：$J_{\min} =$ **111.0 米**（精确计算值为 110.974 米）。
* **可行域模型**：
  * 保证接收区：$C_{\text{recv}} = \bigcap_{g \in \Omega_1} B(g, L(g))$，其中 $L(g) = \max(1000, \|g - S_1\|)$；
  * 严格证明了无论真实干扰源在示向楔形内的任意深度，且无论有效接收半径 $R \in [1000, 1500]$ 是何值，在 $S_2$ 均能 100% 保证收到信号。

---

## 2. 评委审稿必改项（修改清单）

1. **修正 4.2 节关于“三圆精确覆盖”的数学表述**：
   * **原稿问题**：原稿声称“三个端点圆的交集能精确等价于所有连续源的交集”。这在数学上是不严格的（凸包在曲率边界处不等价）。
   * **修改要求**：把“精确等价/充要条件”改为**“保守充分包络（Conservative Sufficient Condition）”**。说明三圆叶子区域是保证可听区域的内包络，物理上具备 100% 可听的充分保证。
2. **修正表 4 的数字标注文案**：
   * **原稿问题**：表 4 中的中心点标为 111.29 米，并写成“本文最优点”；
   * **修改要求**：111.29 米是早前网格粗筛的回退点，必须写清理论最优点是 **111.0 米 @ (844, ±544)**。
3. **附录禁止暴力灌入 600 行代码**：
   * **修改要求**：附录只需保留 `problem2_model.py` 的算法核心流程或伪代码（约 30~50 行），不要把整个无缩进的 Python 脚本无脑贴在附录后面凑页数。
4. **清理凑数参考文献**：
   * 删掉正文中只为引用而引用的 [1][2][3] 凑数文献，统一采用文末提供的规范 GB/T 7714 参考文献。
5. **与问题 3 的衔接用词规范**：
   * 在问题 3 中，**不要写“问题 3 按照问题 2 的最优第二检测点进行部署”**！
   * 因为问题 2 求解的是静态单源的最坏直径最优点（距离 1004m、偏角 32.8°）；而问题 3 是多源在线动态搜索，机器狗补站是采用“只要进入保证接收区 $C_{\text{recv}}$ 就近交会”，避免多走冤枉路。两者的逻辑关系是：**问题 2 为问题 3 提供了保证接收的几何安全域边界**。
