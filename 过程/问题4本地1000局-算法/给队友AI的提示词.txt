你是执行员，不是研究员。现在截稿很紧，只做下面这件事。

【唯一任务】
解压「问题4-本地1000局-定稿算法.zip」，进入 code 目录，安装 numpy，然后只运行：

python run_q4_monte_carlo.py --n 1000 --fidelity fast

【硬性禁令，违反就是失败】
1. 禁止修改任何 .py，禁止改参数、换算法、重写、优化、加对照。
2. 禁止连接官方模拟器，禁止访问网络（装 numpy 除外）。
3. 禁止跑 10 局/100 局试探，禁止跑 10000 局，就是 1000 局一次跑完。
4. 禁止另开实验、禁止读代码后“改进”、禁止换种子。
5. workers 默认用满 CPU，不要手改。
6. 报错只修环境（Python 路径、numpy）。修完仍跑上面同一条命令。
7. 算法已定稿：N19 + 逆时针顺流 + 1300m 扇区延迟 + 奔袭静默。你不需要理解它。

【跑完只做这件事】
把终端打印的统计表，以及 results/ 下这两个文件发回：
- q4_n19_mc_1000_trials.csv
- q4_n19_mc_1000_summary.json

预计 8 核 15–25 分钟。期间不要停。
